# Review Decisions

Cumulative per-finding triage history. Each entry records a Defer/Ignore/Tech-Debt decision from a `/review` cycle. Used by `/review` Phase 6 Reality Filter Q1 (repeat-offender check) on subsequent runs.

This file is NOT a substitute for ADRs. ADRs document permanent design choices (`documentation/decisions/README.md`); entries here document per-cycle triage calls that may evolve.

Last cycle: 1 (2026-05-31)

---

## Cycle 1 - 2026-05-31

### CF-018 at getBaseUrl (security / open-redirect-if-misconfigured)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Validate/allowlist the configured base URL (conditional, low likelihood).
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. `getBaseUrl` in `src/lib/kv-keys.ts` now validates the configured custom domain before using it as the base URL.

### CF-019 at vault routes (security / CSRF via header synthesis, Origin-allowlist reliant)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Add explicit CSRF token on state-changing vault routes; Origin-allowlist currently mitigates.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. An independent double-submit CSRF token (`CSRF_COOKIE_NAME`/`CSRF_HEADER_NAME`/`X-Vault-Csrf`) was added in `src/lib/access.ts` + `src/routes/vault.ts`/`vault-html.ts`, no longer relying on the Origin allowlist alone.

### CF-020 at GitHub OAuth handler (security / 502 detail disclosure)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Return a generic error to the client; log details server-side only.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Already compliant; no change needed. `src/routes/github-auth.ts:138-167` already returns generic client-facing messages and logs the details server-side.

### CF-021 at PROTECTED_PATHS (security / dotfiles browsable)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Populate `PROTECTED_PATHS` if multi-tenant assumptions change; container is single-tenant (AD8/AD29 trust model).
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Accepted-deferred. `PROTECTED_PATHS` is empty by design: the container is single-tenant and the user already owns the filesystem (AD8/AD29 trust model), so there are no other-tenant dotfiles to protect. Revisit only if the trust model changes.

### CF-022 at Env interface (architecture / god-interface)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Split `Env` by subsystem when it next forces a churned change; monitor-only.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Accepted-deferred (monitor-only). The `Env` god-interface is left as-is this cycle; revisit when it next forces a churned change rather than splitting speculatively.

### CF-023 at circuit-breaker cleanup (architecture / performance, amortized on global counter)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Move cleanup to a dedicated timer/alarm if counter growth becomes an issue; monitor-only.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Accepted-deferred, and largely superseded. The amortized-cleanup concern is now moot for unbounded growth: #151/CF-023 added an LRU max-size cap to the per-container breakers (with a new `resetContainerBreakersForReset()`), so the breaker map can no longer grow without bound. A dedicated timer/alarm remains a future option only if profiling shows the counter-amortized path is still a hotspot.

### CF-026 at silent corrupt-data catches (code-comment hygiene)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Document each silent-catch rationale inline; behavior is intentional.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved by the code lanes. The silent corrupt-data catches now carry inline rationale comments documenting why the catch is intentional.

### CF-047 at coverage config (CI-policy / 50% threshold permissive)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Raise the coverage threshold incrementally.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved incrementally. `vitest.config.ts` raised statements/lines/functions 50 -> 53 and branches 40 -> 43; `web-ui/vitest.config.ts` raised statements/lines 30 -> 32 and functions/branches 25 -> 27. CI is the gate.

### CF-050 at documentation/decisions/README.md (doc vocab / "Active" vs "Accepted")
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Normalize ADR status vocabulary.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. Every `**Status:** Active` / `Active (date)` in `documentation/decisions/README.md` was normalized to `Accepted` (dates preserved).

### CF-051 at CF-NNN index (doc hygiene / index gaps, no tombstones)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Add tombstone rows for removed/renumbered CF-NNN entries.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. A "Cycle 1 - CF-NNN tombstones" subsection was added below documenting the numeric gaps in the cycle-1 deferred list so the sequence is self-documenting.

### CF-052 at bisync-trigger description (doc accuracy)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Correct the `bisync-trigger` description.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. `documentation/lanes/architecture.md` now names the user-facing manual trigger as the Worker route `POST /api/sessions/sync` (which fans out per-session), with `POST /internal/bisync-trigger` correctly described as the per-container host endpoint it reaches.

### CF-053 at architecture-internals (doc coverage / single-REQ)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Backfill REQ coverage for architecture-internals.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. The Specification Coverage section in `documentation/lanes/architecture-internals.md` now also backlinks REQ-AUTH-013 (LoginPage) and REQ-SUB-017 (SubscribePage), the REQs whose implementation it describes inline, instead of a single REQ.

### CF-054 at gemini-pro-latest alias (doc caveat / no volatility note)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Add a volatility caveat for the rolling alias.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. `documentation/lanes/container.md` now carries a rolling-alias volatility caveat noting that `gemini-pro-latest` re-points to Google's current flagship without notice and is not a stable contract.

### CF-067 at REQ-SUB-018 AC1 (UI cosmetic / progress bar vs ring)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Amend AC1 to the implemented ring, or change the UI to a bar.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved (no edit needed). REQ-SUB-018 AC1 in `sdd/spec/subscription.md` already reads "progress ring", matching the implemented UI; the spec was already aligned.

### CF-070 at REQ-AUTH-013 AC2 (spec-vs-impl / login label drift)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Align the login-label text in spec or UI.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved. REQ-AUTH-013 AC2 in `sdd/spec/authentication.md` now reads a "Continue with <provider>" button (per-provider), matching the actual `Continue with {provider.name}` label in `web-ui/src/components/LoginPage.tsx`.

### CF-072 at @impl/@test anchors (spec-hygiene / anchor drift, behavior present)
- **Decision:** Tech-Debt
- **Reason:** Triaged to grouped technical-debt GitHub issue (cycle 1, /review --deep --all --verify-high).
- **Suggested action:** Reconcile the `@impl`/`@test` source anchors; CQ-SOURCE / Pass 15 will auto-flag next cycle anyway.
- **Sunset hint:** If 3 cycles in a row see this same decision, consider promoting to ADR.
- **Resolution (technical-debt branch):** Resolved the drifted anchors. REQ-VAULT-009 `@impl` anchors for `maybeSynthesizeCsrfHeader` and `inferOriginValidated` were repointed from `src/routes/vault.ts::` to `src/routes/vault-html.ts::` (where they are now defined), and `src/routes/vault-html.ts::maybeIssueCsrfCookie` (CF-019 double-submit cookie) was added. REQ-SESSION-010 gained `src/routes/session/lifecycle.ts` and `src/lib/kv-keys.ts::reconcileStaleStatus` (Lane B read-side staleness reconciliation). `getBaseUrl` was hardened in place (still `src/lib/kv-keys.ts`) and has no spec `@impl` anchor, so no anchor change was required there.

### Cycle 1 - CF-NNN tombstones (CF-051)

This ledger records only Defer/Ignore/Tech-Debt calls. The cycle-1 `/review --deep --all --verify-high` pass numbered findings CF-018 through CF-072; the IDs below are absent from the deferred list above because they were resolved in-cycle (fixed immediately rather than deferred) or were not Defer/Ignore/Tech-Debt decisions. They are tombstoned here so the CF-NNN sequence is self-documenting and a future cycle does not re-mint a used number:

- CF-024, CF-025: used in cycle 1, not deferred (no triage row here).
- CF-027 through CF-046: used in cycle 1, not deferred (no triage rows here).
- CF-048, CF-049: used in cycle 1, not deferred (no triage rows here).
- CF-055 through CF-066: used in cycle 1, not deferred (no triage rows here).
- CF-068, CF-069: used in cycle 1, not deferred (no triage rows here).
- CF-071: used in cycle 1, not deferred (no triage row here).
