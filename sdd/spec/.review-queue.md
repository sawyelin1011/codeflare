# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

_No open findings._

## Notes — resolved dispositions (kept to prevent re-firing)

### AUDIT-CHAIN-2026-06-21 — sdd-clean commit-body audit tokens (R2-001 / R2-002, resolved)

The `[sdd-clean]` commits of the 2026-06-21 /sdd clean run on `multiview` (`1bac48b`, `51aeb7a`, `ee278a4`, `dfbe8d7`) did not carry Step-0c.5-conforming `spec-enforce: ran (... anchors verified ...)` / `doc-enforce: ran (... anchors verified ...)` tokens — the first three had none; `dfbe8d7` had descriptive metrics but not the literal `anchors verified` token. The enforcement passes demonstrably ran (validators: 0 standalone block anchors, AC-cap ≤7 everywhere, CQ-TEST 297/299, 0 broken backlinks). Those commits are pushed and cannot be amended. **Resolution:** the corrective commit on 2026-06-21 carries conforming audit tokens with the literal `anchors verified` phrase, and all future `[sdd-clean]` commit bodies include conforming tokens. Future runs should not re-fire `enforcement-skill-not-invoked` for these historical commits.

### FINDING-2026-06-15-001 — REQ-AGENT-023 AC3 local-graphify-labels.sh (resolved)

REQ-AGENT-023 AC3's coverage-gap now explicitly names `local-graphify-labels.sh` as integration/manual-only (the script's prepare/apply label round-trip has no automated test; the bounded-refresh wrapper is covered by `safe-graphify-update.test.js`). The convention (every AC carries `@impl` + (`@test` OR `coverage-gap`)) is satisfied.

## 2026-06-22 - PR MERGED un-acked at merge/close
- PR for branch `develop` reached MERGED with un-acked HEAD `3931ae0` (last ack: `43e8786`). Review pipeline did not complete before merge.
