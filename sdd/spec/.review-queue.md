# Review Queue

Layout-resolved triage file for the nested SDD layout. Holds open PR-boundary review findings only; resolved findings are removed (git history is the audit trail).

## Open

### FINDING-2026-06-15-001 — LOW — missing-test-anchor (CQ-TEST gap)

**Finding ID:** missing-test-anchor-1 (2026-06-15)
**Category:** missing-test-anchor
**Affected:** REQ-AGENT-023 | sdd/spec/agents.md lines 1667, 1671

**Rule:** spec-enforce CQ-TEST — every `@impl` anchor on an Implemented REQ should have a corresponding `@test` annotation that covers it (or a note explaining why the script is not directly testable).

**REQ:** REQ-AGENT-023 (`sdd/spec/agents.md` line 1661)
**Severity:** LOW
**File/line:** `sdd/spec/agents.md` lines 1667 and 1671

**Evidence:** The two new `@impl` anchors added by this delta —

```
<!-- @impl: preseed/agents/claude/plugins/graphify/scripts/local-graphify-labels.sh -->
<!-- @impl: preseed/agents/pi/scripts/local-graphify-labels.sh -->
```

— have no paired `@test` annotations pointing at a test that validates the script's behaviour. Existing `@test` lines on REQ-AGENT-023 map to `graphify-active-repo.test.js`, `graphify-mcp-lazy.test.js`, `safe-graphify-update.test.js`, etc., but none cover `local-graphify-labels.sh`.

The `agent-seed-manifest.test.ts` file does assert that `local-graphify-labels.sh` is present in the Pi seed manifest (lines 1374, 1388, 1403) and verifies the `apply .` invocation string, but this test covers Pi-side manifest inclusion only — there is no test asserting the Claude-side script is seeded (`.claude/plugins/graphify/scripts/local-graphify-labels.sh`), and there is no `host/__tests__/` test for the script's runtime behaviour (prepare / apply modes, label round-trip).

**Status:** Implemented is retained — the existing test coverage gap is pre-existing on sibling scripts (`safe-graphify-update.test.js` has a `@test` annotation but the new script does not). This is a LOW annotation gap, not a blocker.

**Proposed fix (for applier):** Add one or both of the following `@test` lines immediately after line 1671 in `sdd/spec/agents.md`, once the corresponding test(s) exist:

```
<!-- @test: src/__tests__/lib/agent-seed-manifest.test.ts (REQ-AGENT-023 Claude-side local-graphify-labels.sh seeded in advanced graphify plugin → AC3 labeling capability) -->
<!-- @test: host/__tests__/local-graphify-labels.test.js (prepare/apply round-trip → advanced-only label pipeline) -->
```

If no host test is planned, annotate the `@impl` line with an inline note explaining the script is exercised only via integration/manual testing, consistent with the approach used on other shell scripts in the graphify plugin directory.

---

_Reviewed: 2026-06-15 | Delta: 3adba28..df74047 | Reviewer: spec-reviewer (report-only)_
